enum ProjectStatus {
    Active
    NotActive
}

model Project {
    id     BigInt        @id @default(autoincrement())
    name   String        @db.VarChar(255)
    status ProjectStatus @default(Active)

    tasks Tasks[]

    @@map("project")
}
